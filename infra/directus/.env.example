# ============================================================================
#  Studio mb · Directus — Variables de entorno
# ----------------------------------------------------------------------------
#  Copia este archivo como `.env` en la misma carpeta del docker-compose.yml
#  y rellena los valores reales. NO commitees el `.env` real.
#
#  ⚠️  Los hosts `postgres` y `redis` son ALIAS DNS de los contenedores
#      `postgres-global` y `redis-global` dentro de la red `servicios-internos`.
#      Funcionan porque Directus está conectado a esa red (ver docker-compose).
# ============================================================================

# --- Claves de aplicación ---------------------------------------------------
# Genera claves seguras con:  openssl rand -hex 32
KEY=                 # 64 chars hex — REEMPLAZAR
SECRET=              # 64 chars hex — REEMPLAZAR

# --- BBDD PostgreSQL GLOBAL --------------------------------------------------
# Esta BBDD ya existe en el servidor. Directus NO la crea.
# Antes de arrancar por primera vez:
#   1) Crear la BBDD  studio_mb_calendario  en postgres-global.
#   2) Crear usuario  studio_mb  con permisos sobre esa BBDD.
# Ver comandos en: docs/SETUP-VPS.md
DB_CLIENT=pg
DB_HOST=postgres              # alias de postgres-global en servicios-internos
DB_PORT=5432
DB_DATABASE=studio_mb_calendario
DB_USER=studio_mb
DB_PASSWORD=                  # REEMPLAZAR
DB_SSL=false

# --- Redis GLOBAL (cache + sesiones) -----------------------------------------
REDIS=redis://redis:6379     # alias de redis-global en servicios-internos
CACHE_ENABLED=true

# --- Admin inicial (solo se aplica la PRIMERA vez al arrancar) ---------------
ADMIN_EMAIL=admin@studiomb.es
ADMIN_PASSWORD=              # REEMPLAZAR
ADMIN_TOKEN=                 # REEMPLAZAR (openssl rand -hex 32)

# --- Acceso público ---------------------------------------------------------
PUBLIC_URL=https://calendar.studiomb.es
TELEMETRY=false

# --- Almacenamiento (uploads locales dentro del contenedor) -----------------
STORAGE_LOCATIONS=local
STORAGE_LOCAL_DRIVER=local
STORAGE_LOCAL_ROOT=/directus/uploads
